<UserControl x:Class="U148.Uwp.Views.RootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="using:U148.Uwp.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="using:U148.Uwp.Views"
             DataContext="{Binding Source={StaticResource Locator}, Path=Root}"
             Loaded="RootView_Loaded"
             RequestedTheme="{Binding Source={StaticResource Locator}, Path=ThemeModeManager.CurrentTheme, Converter={StaticResource ThemeModeToRequestedThemeConverter}}"
             Unloaded="RootView_Unloaded"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:IsNullConverter x:Key="IsNullConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="RootGrid">
        <Border IsHitTestVisible="{Binding ElementName=LoginViewHost, Path=Child, Converter={StaticResource IsNullConverter}}">
            <views:MainView />
        </Border>
        <Border x:Name="LoginViewHost" />
        <Grid x:Name="ToastPromptContainer"
              Padding="0,16,0,0"
              x:FieldModifier="internal" />
    </Grid>
</UserControl>