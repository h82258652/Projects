<UserControl x:Class="VGtime.Uwp.Controls.ForumPostItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="using:WinRTXamlToolkit.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fontawesome="using:SoftwareKobo.Icons.FontAwesome"
             xmlns:localConverters="using:VGtime.Uwp.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:VGtime.Models"
             d:DataContext="{d:DesignInstance Type=models:Post}"
             mc:Ignorable="d">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:BooleanToVisibilityConverter x:Key="InversedBooleanToVisibilityConverter"
                                                     IsInversed="True" />
            <localConverters:TimeAgoConverter x:Key="TimeAgoConverter" />
            <localConverters:LikeNumToStringConverter x:Key="LikeNumToStringConverter" />
            <localConverters:CommentNumToStringConverter x:Key="CommentNumToStringConverter" />
        </ResourceDictionary>
    </FrameworkElement.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Ellipse Grid.Column="0"
                     Width="40"
                     Height="40">
                <Shape.Fill>
                    <ImageBrush ImageSource="{Binding Path=User.AvatarUrl}" />
                </Shape.Fill>
            </Ellipse>
            <Grid Grid.Column="1"
                  Margin="8,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Text="{Binding Path=User.Name}" />
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal">
                    <TextBlock Foreground="Gray"
                               Text="{Binding Path=PublishDate, Converter={StaticResource TimeAgoConverter}}" />
                    <TextBlock Margin="4,0,0,0"
                               Foreground="{ThemeResource VGtimeAccentBrush}"
                               Text="发布了" />
                </StackPanel>
            </Grid>
        </Grid>
        <Grid Grid.Row="1"
              Margin="0,8,0,0">
            <TextBlock Text="{Binding Path=Title}"
                       Visibility="{Binding Path=IsShort, Converter={StaticResource InversedBooleanToVisibilityConverter}}" />
            <TextBlock Text="{Binding Path=Text}"
                       Visibility="{Binding Path=IsShort, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
        <Grid Grid.Row="2"
              Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Viewbox Width="18"
                             Height="18">
                        <fontawesome:FontAwesomeIcon Symbol="HeartOutline" />
                    </Viewbox>
                    <TextBlock Margin="4,0,0,0"
                               Text="{Binding Path=LikeNum, Converter={StaticResource LikeNumToStringConverter}}" />
                </StackPanel>
            </Button>
            <Button Grid.Column="1"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Viewbox Width="18"
                             Height="18">
                        <fontawesome:FontAwesomeIcon Symbol="CommentingOutline" />
                    </Viewbox>
                    <TextBlock Margin="4,0,0,0"
                               Text="{Binding Path=CommentNum, Converter={StaticResource CommentNumToStringConverter}}" />
                </StackPanel>
            </Button>
            <Button Grid.Column="2"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Viewbox Width="18"
                             Height="18">
                        <FontIcon FontFamily="Segoe MDL2 Assets"
                                  Glyph="&#xE72D;" />
                    </Viewbox>
                    <TextBlock Margin="4,0,0,0"
                               Text="分享" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>