<UserControl x:Class="BingoWallpaper.Wpf.Views.DetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}"
             d:DesignWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}"
             DataContext="{Binding Source={StaticResource Locator}, Path=Detail}"
             mc:Ignorable="d">
    <Grid>
        <Panel.Background>
            <SolidColorBrush Opacity="0.7"
                             Color="Black" />
        </Panel.Background>
        <Button Click="GoBackButton_Click"
                Command="{Binding Path=GoBackCommand}"
                Style="{StaticResource EmptyButtonStyle}" />
        <Grid HorizontalAlignment="Center"
              VerticalAlignment="Center">
            <Image x:Name="Image"
                   Loaded="Image_Loaded"
                   Source="https://images2017.cnblogs.com/news/66372/201711/66372-20171102151244701-408309088.jpg"
                   Stretch="None" />
            <Button HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{Binding Path=SaveCommand}" />
        </Grid>
    </Grid>
</UserControl>